export interface Organization {
  id: string
  companyName: string
  companyCode: string // 기업코드
  // rating: string
  // industry: string // 산업
  // numberOfEmployees: number // 사원수
  // companyType: string // 기업 구분 (ex. 대기업, 중소기업 등)
  // establishmentDate: string // 설립일 (ISO 날짜 문자열로 처리)
  // capital: number // 자본금 (단위: 원)
  // ceoName: string // 대표자
  // graduateSalary: number // 대졸 초임 (연봉 또는 월급)
  // mainBusiness: string // 주요 사업
  // hasFourInsurances: boolean // 4대 보험 가입 여부
  // homepage: string // 홈페이지 URL
  // address: string // 본사 주소
  // affiliates: string[] // 계열사 목록
}
